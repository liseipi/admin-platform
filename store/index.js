export const state = () => ({
  // cartData: {}
})

export const mutations = {
  // SET_CartData(state, data) {
  //   state.cartData = data
  // }
}

export const getters = {
  // SET_CartData(state) {
  //   return state.cartData
  // }
}
